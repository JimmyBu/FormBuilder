import React from "react";
import FormBuilder from "./form_builder"; // Assuming FormBuilder is in the same directory

const App = () => {
  return (
    <div>
      <FormBuilder />
    </div>
  );
};

export default App;
